{
  "name": "chrisbridewell-infrastructure",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "infrastructure"
  ],
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "synth": "npm run synth --workspace infrastructure"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
